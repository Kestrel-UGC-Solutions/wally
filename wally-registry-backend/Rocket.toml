[default]
address = "0.0.0.0"
port = 8000
auth = { type = "api_key", api_key = "${WALLY_API_KEY}" }
storage = { type = "gcs", bucket = "wally-registry-bucket-kestrel" }
index_url = "https://github.com/Kestrel-UGC-Solutions/wally-index"

[release]
log_level = "normal"
